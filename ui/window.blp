using Gtk 4.0;
using Adw 1;

template $AppWindowsWindow : Adw.ApplicationWindow {
  default-width: 1200;
  default-height: 760;

  Adw.ToolbarView toolbar_view {
    content: Gtk.Stack content_stack {
      vexpand: true;
      hexpand: true;
    };
  }
}